import React from 'react';
// import { useRoutes } from 'react-router-dom';
// import routes from '../routes';

export default function App(): React.ReactElement | null {
  // return useRoutes(routes);

  return <h1>Teste Inicial Branch</h1>
}
